import { Box } from "@mantine/core";

const PrintIssueDiplomas = ({ innerRef }: { innerRef: any }) => {
  return (
    <div ref={innerRef}>
      <Box>In ra cái này</Box>
    </div>
  );
};

export default PrintIssueDiplomas;
